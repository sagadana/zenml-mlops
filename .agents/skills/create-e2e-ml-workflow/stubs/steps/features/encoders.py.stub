"""ZenML step: build_encoders."""
from __future__ import annotations

from typing import Annotated

import pandas as pd
from zenml import step


@step(enable_cache=True)
def build_encoders(
    processed_ratings: pd.DataFrame,
) -> tuple[
    Annotated[pd.Series, "user_encoder"],
    Annotated[pd.Series, "item_encoder"],
]:
    """
    Build dense integer encoders for primary entities.
    Encoding is applied to features separately in prepare_features.
    """
    raise ValueError("Customize build_encoders() for your entity ID columns.")
