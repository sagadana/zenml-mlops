"""ZenML step: ingest_data."""
from __future__ import annotations

from typing import Annotated

import pandas as pd
from zenml import step


@step(enable_cache=True)
def ingest_data(dataset_size: str = "small") -> Annotated[pd.DataFrame, "raw_ratings"]:
    """
    Load raw source data and return a pandas DataFrame.

    Customize this for your dataset source (API, S3, database, file, etc.).
    """
    raise ValueError(
        "Customize ingest_data() with source-specific loading and return a pandas DataFrame."
    )
