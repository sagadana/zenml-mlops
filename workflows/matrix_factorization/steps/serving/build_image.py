# Re-exported from the global step — kept for backward compatibility.
# New workflows should import directly from steps.serving.build_image.
from steps.serving.build_image import build_serving_image  # noqa: F401
